{% extends 'admin/base.html.twig' %}

{% block title %}{% trans from 'messages+intl-icu' %}admin.users.main{% endtrans %}{% endblock %}

{% block stylesheets %}
    <style>

    </style>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-3 col-6 m-auto">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">{% trans from 'messages+intl-icu' %}admin.create{% endtrans %}</h3>
                </div>
                {{ form_start(CreateUserForm) }}
                <div class="card-header">
                    <h3 class="card-title">{{ form_errors(CreateUserForm) }}</h3>
                </div>
                <div class="card-body">
                    <div class="form-group">
                        {{ form_label(CreateUserForm.email) }}
                        {{ form_widget(CreateUserForm.email, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(CreateUserForm.plainPassword) }}
                        {{ form_widget(CreateUserForm.plainPassword, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(CreateUserForm.roles) }}
                        {{ form_widget(CreateUserForm.roles, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="card-footer">
                    {{ form_widget(CreateUserForm.submit, {'attr': {'class': 'btn btn-primary'}}) }}
                </div>
                {{ form_end(CreateUserForm) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}